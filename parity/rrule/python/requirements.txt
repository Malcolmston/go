# The oracle, pinned exactly. An unpinned oracle makes the score
# unreproducible, so parity_test.go refuses any interpreter whose
# dateutil.__version__ is not this.
#
# The harness uses the first interpreter that carries it, preferring a venv at
# parity/rrule/python/.venv over the system python3:
#
#   python3 -m venv parity/rrule/python/.venv
#   parity/rrule/python/.venv/bin/pip install -r parity/rrule/python/requirements.txt
python-dateutil==2.9.0.post0
