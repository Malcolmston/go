# The upstream install is pinned by package.json/package-lock.json, not vendored.
node/node_modules/

# parity/.gitignore ignores parity.json globally (a run artifact of the older
# in-repo harness). Here it is a deliverable: the machine-readable score.
!parity.json
