# frozen_string_literal: true

# Self-contained oracle for the migrate parity harness. The upstream under test
# is rails/rails' ActiveRecord migration layer; SQLite keeps the whole thing
# local so no database server is needed.
source "https://rubygems.org"

gem "activerecord", "8.0.2"
gem "sqlite3", "2.7.4"
