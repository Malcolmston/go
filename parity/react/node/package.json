{
  "name": "react-parity-upstream",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Upstream oracle for parity/react: facebook/react react-dom/server, pinned exactly. The vitest suite here IS the parity gate.",
  "scripts": {
    "test": "vitest run",
    "oracle": "node run.mjs"
  },
  "dependencies": {
    "react": "19.2.7",
    "react-dom": "19.2.7"
  },
  "devDependencies": {
    "vitest": "4.1.10"
  }
}
